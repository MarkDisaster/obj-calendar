/**
 * Props for the Popover component.
 */
export interface PopoverProps {
   /**
    * Indicates whether the popover is currently visible.
    * @type {boolean}
    */
   isVisible: boolean;

   /**
    * The text content to be displayed inside the popover.
    * @type {string}
    */
   text: string;
}
